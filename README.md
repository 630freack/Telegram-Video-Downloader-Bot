# Telegram Video Downloader Bot

Этот бот позволяет скачивать видео по ссылкам и управлять файлами через Telegram.

## Функции

🔐 **Аутентификация и безопасность**
- Только один администратор по ID
- Все остальные пользователи блокируются

📥 **Загрузка видео**
- Принимает прямые ссылки на видео
- Проверка доступности ссылки
- Автоопределение имени файла
- Выбор папки для сохранения
- Создание новых папок
- Прогресс-бар загрузки

📂 **Файловая система**
- Просмотр папок
- Создание папок
- Переименование файлов

💾 **Хранение данных**
- История загрузок в SQLite
- Настройки в конфиге

📤 **Отправка через UserBot**
- Отправка файлов до 2 ГБ (4 ГБ с Premium)
- Отправка от имени пользователя

## Установка

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

2. Настройте конфигурацию:
```bash
cp config/config.py.example config/config.py
```

3. Заполните свои данные в `config/config.py`

4. Запустите бота:
```bash
python bot/main.py
```

## Конфигурация

Создайте `config/config.py` с следующими параметрами:
- `BOT_TOKEN` - токен от @BotFather
- `ADMIN_ID` - ваш Telegram ID
- `API_ID` и `API_HASH` - от my.telegram.org
- `STORAGE_PATH` - путь для загрузок
- `DATABASE_PATH` - путь к базе данных

## Команды

- `/start` - приветствие
- `/download <ссылка>` - начать загрузку
- `/folders` - список папок
- `/create_folder <название>` - создать папку
- `/rename <старое> to <новое>` - переименовать файл
- `/send <файл> to <получатель>` - отправить через аккаунт
- `/history` - история загрузок